{
  "name": "nv-tradingview-webhook",
  "version": "1.0.0",
  "description": "TradingView → MetaApi → MT5 TMS webhook server",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
